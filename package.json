{
  "private": true,
  "name": "prettyprint-format",
  "version": "0.0.1",
  "description": "General-purpose pretty print routine modeled after Format module in OCaml",
  "main": "lib/index.js",
  "scripts": {
    "browserify": "browserify -s Format lib/index.js -o prettyprint-format.js",
    "test": "mocha",
    "testmake": "node test-src/makeTest.js -o test/compatibility.spec.js -i test-src/testDataSet.json",
    "testmakerun": "npm run testmake && npm run test"
  },
  "keywords": [
    "pretty print",
    "format",
    "text"
  ],
  "author": "izreit",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^10.2.0",
    "chai": "^2.3.0",
    "commander": "^2.8.1",
    "ect": "^0.5.9",
    "mocha": "^2.2.4"
  }
}
